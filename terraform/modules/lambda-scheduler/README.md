# Lambda Scheduler Module

This module creates a Lambda function for scheduling ECS service start/stop operations.

## Features

- Start/stop ECS services on schedule
- CloudWatch custom metrics for monitoring
- Error handling and logging
- Configurable desired task count

## Usage

See the staging environment configuration for example usage with terraform-aws-modules/lambda/aws.
